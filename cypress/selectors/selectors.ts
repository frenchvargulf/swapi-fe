export enum GameSelectors {
    Winner = '[data-cy="winner"]',
    LeftCardMass = '[data-cy="left-card"] .property',
    RightCardMass = '[data-cy="right-card"] .property',
    PlayAgainButton = '[data-cy="play-again"]',
    LeftCardWinCount = '[data-cy="left-card-win-count"]',
    RightCardWinCount = '[data-cy="right-card-win-count"]',
    ChangeModeButton = '[data-cy="change-mode"]'
}
